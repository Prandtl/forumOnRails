#posts
  - @posts.each do |post|
    .post
      %h2.post_name= link_to post.title, post

      %p.post_info
        Published at:
        = time_ago_in_words(post.created_at)
        by
        %span.post_autor
          = post.user.username
